# 변경 기록

이 파일은 사용자가 **버전을 올릴지 판단할 때** 보는 것이다. 그래서 무엇이 바뀌었는지보다
**무엇이 깨져 있었는지**를 먼저 적는다. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/),
버전은 [유의적 버전](https://semver.org/lang/ko/)을 따른다.

## [0.8.2] — 2026-08-27

### 문서

- **README 두 판이 낡은 검증 수치를 적고 있었다.** «테스트 276건, R1~R23, P1~P26»
  이라고 했는데 실제로는 416건이었고 `render.test.ts` 에 **S1~S10 이 통째로 더** 있었다.
  0.7.0 에서 시퀀스 검사 열 개를 넣으면서 README 를 안 고쳤다.
  이 저장소는 같은 이유로 한 번 지적받은 적이 있다(`d59d3dc`, 코덱스 9차).

### 검사

- **P28** — README 두 판이 말하는 검사 계열 범위가 실제와 맞는지 본다. 소스에서
  `A`, `R`, `S`, `P` 계열의 최댓값을 긁어 한글판(`~`)과 영문판(`–`) 표기를 대조한다.
  숫자를 손으로 맞추는 한 또 낡으므로 **계열 범위만이라도 기계가 지키게** 했다.
  양방향 변이로 확인했다 — README 를 낡게 만들어도, 검사만 늘리고 문서를 안 고쳐도 걸린다.
- 총 건수는 기계가 안 본다. 변이 루프처럼 **실행 중에 만들어지는 검사**가 있어 소스만
  읽어서는 셀 수 없다. 그래서 README 에는 «0.8.2 기준» 이라고 시점을 함께 적었다.
- ⚠️ «소스 변이 54종» 은 손으로 관리하는 수치라 이번에 손대지 않았다. 확인할 방법이 없다.

## [0.8.1] — 2026-08-27

### 고침

- **메시지 번호가 혼자 한 줄을 차지하던 것을 고쳤다.** 번호를 라벨에 이어 붙인 뒤
  통째로 접었더니, 라벨이 공백 없는 긴 토큰일 때 이렇게 됐다.

  ```
  1.
  /push?PUSH_DATA=..&keyversion=v3&
  USE_INTT_ID=PSNM_1
  ```

  행 하나를 통째로 잃고 그 아래 행이 전부 밀린다. 이제 번호는 접기에 참여시키지 않고
  첫 줄에 붙인다. 같은 그림 실측으로 **1429×1027 → 1441×993** (세로 34 줄고 가로 12 늘어남).
- **한 열짜리 설명 상자를 늘 생명선 오른쪽에만 붙이던 것을 고쳤다.** 왼쪽 간격이 이미
  넓어 자리가 남아도 안 쓰고 오른쪽 열을 밀었다. 이제 **덜 벌리는 쪽**에 붙인다.
  양쪽이 같으면 오른쪽이다(읽는 방향). 첫 열은 왼쪽에 열이 없어 오른쪽, 마지막 열은
  흡수할 간격이 없어 왼쪽이 유리하다. 실측으로 **1441×993 → 1331×993** (가로 110 줄고 세로 그대로).
- 설명 상자 연결선을 **가까운 변**까지만 긋는다. 먼 변까지 그으면 상자 밑을 가로지르는
  선이 생긴다 — 상자가 나중에 그려져 가려질 뿐 사라진 게 아니다.

### 검사

- **임시폴더 검사가 총량을 세다 깨져 있었다.** 렌더는 시작할 때마다 24시간 지난
  폴더를 치우므로, 그런 잔여가 있으면 개수가 늘면서 동시에 줄어든다. 실측으로
  113개 중 24~48시간짜리가 섞여 있어 «하나 늘어야 한다» 가 0 으로 나왔다.
  깨끗한 main 에서도 같은 실패가 났으니 소스 문제가 아니었다.
  이제 **이름 집합의 차이**로 새로 생긴 것만 센다. R16 과 같은 교훈이다 —
  전역 자원은 총량이 아니라 내 몫의 증분으로 본다. 양방향 변이로 검출력을 확인했다.

### 재본 뒤 넣지 않은 것

- 설명 상자만 더 넓게 접어 세로를 줄여보려 했는데 **측정이 기각했다.** 폭 250 에서
  320, 380, 440, 520 으로 올려도 세로는 1027 에서 960 까지(6.5%)밖에 안 줄고 가로가
  1429 에서 1930 까지(35%) 늘었다. 설명 상자 높이가 전체 세로를 지배한다는 전제가 틀렸다.

## [0.8.0] — 2026-08-27

### 고침

- **긴 라벨이 식별자 한가운데서 갈라지던 것을 고쳤다.** `wrapText` 가 공백으로만
  자르고 공백 없는 토큰은 곧장 글자 단위로 내려갔다. 그래서
  `…&keyversion=v3&USE_INTT_ID=PSNM_1` 이 `USE` 와 `_INTT_ID` 로 쪼개졌다.
  이제 글자 단위로 가기 전에 구분 기호(`& ? = / , ; |`) 뒤에서 먼저 끊는다.
  `_` `.` `-` 는 일부러 뺐다 — 식별자 안에 흔해서 거기서 끊으면 글자 단위와 다를 게 없다.
  구분 기호가 없는 한글 토큰은 예전 경로 그대로다.
- 0.7.4 에서 «접을 자리를 직접 끊어라» 라고 안내했던 것을 걷었다. 그 몫이 렌더러로
  넘어왔다. 이제 직접 끊어야 하는 건 구분 기호가 없는 긴 한글 토큰뿐이다.

⚠️ **이 결함은 자가감사를 통과했다.** 줄이 제 상자 안에 있고 겹치지도 않아 기하로는
결함이 아니다. 그래서 검사(S8)를 렌더가 아니라 계산식 단위로 걸었다 — S7 과 같은 이유로
폰트를 전제하면 다른 기계에서 조용히 헛돈다. 구분 기호 목록을 비우는 변이로 검출력을 확인했다.

## [0.7.4] — 2026-08-26

### 고침

- **시퀀스 도구가 처음부터 깔끔하게 나오도록 작성 요령을 도구 설명에 넣었다.** 실제로
  구성도를 하나 그려보니 첫 판이 지저분했는데, 원인은 렌더러가 아니라 **입력**이었다.
  같은 그림을 2904×2508 에서 2370×2178 로 줄인 건 셋뿐이다 — `call` 마다 `return` 을
  붙여 활성 막대를 닫고, `note` 를 한 줄로 줄이고, 긴 라벨의 접을 자리를 직접 끊는 것.
  이 요령이 설명에 없어서 부르는 쪽은 매번 첫 판을 다시 만들게 돼 있었다.
  `messages` 필드 설명과 `docs/tools.md` 에도 같이 적었다.
- 함께 적었다: **자가감사 통과는 「보기 좋다」가 아니다.** 계단처럼 쌓인 막대도 어색하게
  끊긴 라벨도 겹침 0건으로 통과한다. 감사는 렌더러의 배치 계산이 틀렸는지만 본다.

렌더러 코드는 바뀌지 않았다. 같은 입력을 주면 같은 그림이 나온다.

## [0.7.3] — 2026-08-24

- 문서만 갱신. `docs/decisions/0006-ship-as-plugin.md` 의 도구 개수가 0.7.0 이후로 낡아
  있었다. 이 레포는 지난 결정 기록을 고쳐 쓰지 않고 배너로 정정하므로(ADR 0001, 0002,
  0004 전례) 본문은 두고 머리에 배너를 달았다. `docs` 가 `files` 에 걸려 npm 으로
  실려 나가는 파일이라 패치 릴리스로 내보낸다. `dist` 는 0.7.2 발행본과 동일하다.

## [0.7.2] — 2026-08-24

- MCP 레지스트리 목록에 뜨는 한 줄 설명에 시퀀스 다이어그램이 빠져 있었다. 0.7.0 에서 도구를
  넣으면서 README 와 플러그인 설명은 고쳤는데 `server.json` 만 못 고쳤다. 레지스트리를 찾아
  들어오는 사람은 이 한 줄만 보고 판단하므로, 그 사람 눈에는 그림 도구가 없는 것이나 같았다.
  `description` 에 100자 상한이 있어 문구를 줄여 넣었고, 공개 발행이 선택이라는 안전 문구는
  그대로 뒀다. `VELOG_CHROME_PATH` 설명도 시퀀스를 포함하도록 고쳤다.
  `dist` 는 0.7.1 발행본과 동일하다 — 동작은 바뀌지 않는다.

## [0.7.1] — 2026-08-24

- 문서만 갱신. npm 에 실려 나가는 `docs/tools.md` 와 `CHANGELOG.md` 가 0.7.0 발행 뒤에
  바뀌었는데, npm 은 같은 버전 재발행이 안 되므로 패치 릴리스로 내보낸다. 0.4.1 과 같은 이유다.
  `dist` 는 0.7.0 발행본과 동일하다 — 동작은 바뀌지 않는다.

## [0.7.0] — 2026-08-24

### 추가

- **시퀀스 다이어그램 도구 `velog_render_sequence` 를 넣었다.** 기존 `velog_render_diagram`
  으로도 좌표를 손으로 주면 그려지긴 했다. 그런데 구성도는 좌표가 자유로운 반면
  시퀀스는 열이 참가자, 행이 시간이라 좌표에 의미가 있다. 같은 도구에 얹으면 메시지
  하나를 끼워 넣을 때마다 그 아래 y 를 전부 다시 계산해야 한다. 그래서 도구를 나눴고,
  이쪽은 **좌표를 아예 받지 않는다** — 참가자와 순서 있는 메시지만 받고 열 간격,
  행 높이, 라벨 줄바꿈, 활성 막대, 묶음 상자를 전부 실측으로 정한다.

  글자를 줄여 맞추지 않고 **글자에 맞춰 자리를 넓힌다.** 라벨이 두 열 사이에 안 들어가면
  그 구간의 열 간격을 늘리고, 길면 접고, 접힌 만큼 그 행을 높인다.

  `velog_render_diagram` 은 손대지 않았다. 좌표계가 달라서 한 도구에 담을 수 없다.

### 알아둘 것

- 기존 도구로 생명선을 그리면 **끝에 화살촉이 달린다.** 모든 선에 `marker-end` 를
  무조건 붙이기 때문이고, 이게 도구를 나눈 이유 중 하나다. 새 도구는 붙이지 않는다.
- 자가감사는 기하만 본다. 만들면서 나온 결함 여섯 개 중 **셋은 감사를 통과한 채**
  눈으로 잡았다(칩이 활성 막대에 가려짐, 배지가 이름을 덮음, 묶음 상자가 관계없는
  열까지 감쌈). 통과가 곧 좋은 그림이라는 뜻이 아니다.

## [0.6.1] — 2026-08-23

### 추가

- **공식 MCP 레지스트리에 등록할 수 있게 했다.** `registry.modelcontextprotocol.io` 는
  npm 에 게시된 패키지의 `mcpName` 을 읽어 소유권을 검증한다. 그 필드가 없어서
  등록 자체가 불가능했다. `package.json` 에 `mcpName`, 저장소 루트에 `server.json` 을 넣었다.
  이름은 `io.github.milcho0604/velog` 다.

### 고침

- **도구 설명이 이미 고친 버그를 계속 설명하고 있었다.** `velog_update_post` 가 모델에게
  "현재 설정에서는 공개 글을 수정하면 비공개로 내려간다"고 말했는데, 그건 0.4.2 에서
  고친 동작이다. 구현은 공개 범위를 잇고 회귀 테스트가 그걸 강제하는데 **설명만 옛날
  그대로였다.** 모델이 읽는 텍스트라 안 해도 될 걱정을 시켰다.
- **npm 에 실려 나가는 `docs/tools.md` 의 서술 네 곳이 구현과 반대였다.** 발행을 못 한다,
  초안에 발행 제한이 걸린다, `velog_update_draft` 가 발행글을 강등한다, 게이트가 꺼지면
  공개글이 내려간다 — 전부 지금은 사실이 아니다. ADR 0004 의 '대가' 절에도 정정을 붙였다.

### 변경

- **npm 설명문을 다시 썼다.** 벨로그 MCP 서버가 넷인데 넷 다 "글을 읽고 쓴다"만 적고 있었다.
  공개 발행을 모델이 못 켠다는 것과, 문서 없는 벨로그 GraphQL 함정을 실측해 정리했다는 것을
  앞세웠다. 검색 결과에서 사람이 보는 건 이 한 줄뿐이다.

## [0.6.0] — 2026-08-21

### 추가

- **초안을 만들면서 시리즈까지 붙는다.** 벨로그는 임시저장 **생성** 단계에서 `series_id` 를
  버린다(`if (series_id && !data.is_temp)`). 지금까지는 그 비대칭을 알려주고
  `velog_update_draft` 를 직접 부르게 했다. 이제 저장 직후 한 번 더 붙이고,
  **재조회로 확인한 뒤에만** 붙었다고 말한다.
- 붙이기 실패를 두 종류로 나눠 알린다. 아무것도 안 쓴 것이 확실하면 **미적용**,
  수정 요청을 보낸 뒤 확인에 실패했으면 **반영 여부 불명**이다. 후자를 전자로 적으면
  이미 붙어 있는 시리즈를 사용자가 다시 붙이려 든다.

### 변경

- **Node 하한을 `>=24` 에서 `>=22.18.0` 으로 내렸다.** 24 는 근거 없는 선택이었다.
  의존성 중 24 를 요구하는 것이 없고 소스에도 24 전용 API 가 없다. 실제 하한은
  `.ts` 를 플래그 없이 실행할 수 있는 첫 버전이고 그게 22.18.0 이다.
  CI 가 그 **정확한 버전**을 돌린다(`22` 라고 쓰면 최신 22.x 로 풀려 하한을 못 잰다).
- 이름으로 찾은 시리즈는 소유권을 다시 조회하지 않는다. 내 목록에서 고른 id 라 이미
  증명돼 있다. **사용자가 준 `series_id` 는 그대로 검사한다.** 네 도구 모두 적용.
  초안 생성 기준 왕복 7회 → 6회.

### 고침

- **동시 수정이 서로를 덮어쓰던 것.** 후속 붙이기가 생성 전용 줄에서 돌아
  `update_draft` 나 `publish_draft` 와 남남이었다. 늦게 도착하면 방금 발행된 글이
  비공개 초안으로 되돌아가고 **두 도구 다 성공을 보고**했다.
- **되돌아간 수정.** 후속 붙이기가 생성 당시 입력을 다시 펼쳐, 그 사이의 제목과 본문
  수정을 통째로 지웠다. 이제 직전에 읽은 **현재 값**으로 전체교체를 만든다.
- 복구 안내가 실행 불가능했다. `velog_update_draft` 는 `title` 과 `body` 가 필수인데
  안내는 id 만 주라고 했다.
- 취소가 중복 생성을 막는다고 주석에 적혀 있었으나 사실이 아니다. MCP SDK 는 취소되면
  서버 반환값과 무관하게 호출자 Promise 를 reject 한다. 막을 수 없다는 사실을 적었다.

### 문서

- README 두 벌과 `docs/`, 플러그인 설명에서 "Node 24" 표기를 모두 정정했다.
  실행되는 것은 컴파일된 `dist/index.js` 다 — `.ts` 직접 실행은 개발과 검증의 요구사항이다.

## [0.5.0] — 2026-08-13

### 추가

- **썸네일 자동 채움.** 생략하면 본문 첫 이미지를 쓴다. 코드블록 안의 이미지는 제외하고,
  `null` 로 끌 수 있으며, 수정은 기존 썸네일을 덮지 않는다.
- **시리즈를 이름으로 지정**(`series_name`). id 는 사람도 AI 도 모른다. 저장 요청을
  보내기 **전에** 찾아 같은 요청에 싣는다. 못 찾으면 아무것도 쓰지 않고 멈춘다.

### 고침

- **저장은 됐는데 실패로 보고되던 것.** 시리즈 힌트 조회를 인자 자리에서 `await` 해
  `try` 밖에서 평가됐다. 계정 조회가 실패하면 글이 이미 저장된 뒤인데 호출이 통째로
  실패로 나갔고, 재시도하면 **글이 두 번 생겼다.**
- 비워 둔 썸네일이 수정 때 채워지던 것, `series_id: ""` 가 기존 시리즈 연결을 끊던 것.

## [0.4.2] — 2026-08-11

### 고침

- **공개 발행된 글의 제목만 고쳐도 비공개로 내려가던 것.** 공개 게이트가 꺼져 있으면
  `update_post` 가 무조건 `is_private: true` 를 보냈다. 게이트가 막아야 할 것은
  '비공개를 공개로'이지 '공개된 글의 강등'이 아니다. 오류도 아니어서 성공으로 보였다.
- **부분 성공을 완전 실패로 보고하던 것.** `data` 와 `errors` 가 함께 오면 `data` 를
  버리고 던졌다. 사용자는 실패로 보고 다시 불러 **글이 두 번 생겼다.**
- **살아 있는 refresh token 이 축출되던 것.** `Set.add()` 는 기존 값의 순서를 바꾸지
  않는데 삽입 순서를 '오래된 순서'로 썼다.
- **60초 벽 3종.** 재시도 총예산 없음(실측 61.6초), 내보내기 기본값이 77.9초,
  취소 신호를 보는 도구가 0개.
- **동시 수정이 서로를 덮어쓰던 것.** 벨로그에 버전이 없어 나중 쪽이 앞을 지우는데
  둘 다 성공을 보고했다. 대상별로 줄을 세운다.

## [0.4.1] — 2026-08-02

- 문서만 갱신(npx 안내, 웹앱 미지원 설명, npm 배지). npm 은 같은 버전 재발행이 안 되므로
  문서 갱신에도 패치 릴리스가 필요하다.

## [0.4.0] — 2026-08-02

- 첫 npm 공개 발행.

[0.8.2]: https://github.com/milcho0604/velog-mcp/releases/tag/v0.8.2
[0.8.1]: https://github.com/milcho0604/velog-mcp/releases/tag/v0.8.1
[0.8.0]: https://github.com/milcho0604/velog-mcp/releases/tag/v0.8.0
[0.7.4]: https://github.com/milcho0604/velog-mcp/releases/tag/v0.7.4
[0.7.3]: https://github.com/milcho0604/velog-mcp/releases/tag/v0.7.3
[0.7.2]: https://github.com/milcho0604/velog-mcp/releases/tag/v0.7.2
[0.7.1]: https://github.com/milcho0604/velog-mcp/releases/tag/v0.7.1
[0.7.0]: https://github.com/milcho0604/velog-mcp/releases/tag/v0.7.0
[0.6.1]: https://github.com/milcho0604/velog-mcp/releases/tag/v0.6.1
[0.6.0]: https://github.com/milcho0604/velog-mcp/releases/tag/v0.6.0
[0.5.0]: https://github.com/milcho0604/velog-mcp/releases/tag/v0.5.0
[0.4.2]: https://github.com/milcho0604/velog-mcp/releases/tag/v0.4.2
[0.4.1]: https://github.com/milcho0604/velog-mcp/releases/tag/v0.4.1
[0.4.0]: https://github.com/milcho0604/velog-mcp/releases/tag/v0.4.0
